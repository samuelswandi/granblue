# GBF Wiki Drafts

Paste-ready wikitext drafts for `gbf.wiki`.

Files:

- `User-Zynix.wiki`: main `User:Zynix` page draft
- `User-Zynix-GW-History.wiki`: starter `User:Zynix/GW History`
- `User-Zynix-Element-Template.wiki`: starter template for `User:Zynix/Fire`, `Water`, `Earth`, `Wind`, `Light`, `Dark`

Notes:

- `User:Zynix/Profile` and `User:Zynix/SupportSummon` are custom subpage templates and must exist on-wiki before the main page fully renders.
- `{{/Collection Tracker}}`, `{{/GW History}}`, and `{{/Misc_Trackers}}` are transclusions of subpages relative to `User:Zynix`.
- Replace placeholder values before pasting.
